"""Webserver-stack examples package.

Empty marker so the runnable scripts under
``examples/webserver_stacks/`` can be imported as a Python package
during testing (``import examples.webserver_stacks.litellm_perplexity``).
The scripts themselves stay runnable directly via
``uv run python examples/webserver_stacks/<stack>.py``.
"""
