"""Cognitive kernel (plan M2): the conversational agent brain.

The kernel is an LLM tool-calling loop on deepagents (behind the ONE
:mod:`inqtrix.agents.harness` seam, decision E10) registered as a SECOND
``AgentAlgorithm`` next to the deterministic phase machine — which stays
the "deep mission" capability the kernel delegates large researched
deliverables to (ADR-PLAT-2).
"""
